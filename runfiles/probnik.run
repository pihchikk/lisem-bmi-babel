[openLISEM runfile version 7.x]
Map Directory=/home/jovyan/work/LISEM 3/inputfiles/VNIIMZ_10m/maps_probnik_10m/
Result Directory=/home/jovyan/work/LISEM 3/output_probnik/

# core rasters
dem=dem.map
grad=grad.map      # sine of slope angle
manning=n.map      # surface roughness
rr=rr.map          # scalar field required by the model

# a couple of scalars the model reads early (safe defaults)
Infil Method=0
Result datetime=0
